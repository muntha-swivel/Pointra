import { StyleSheet } from "react-native";

const redemptionHistoryStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
});

export default redemptionHistoryStyles;
